services:
  opencode2api:
    build:
      context: .
      dockerfile: Dockerfile
    image: opencode2api:local
    restart: unless-stopped
    environment:
      CONFIG_PATH: /app/config.json
      LISTEN_ADDRESS: 0.0.0.0:8180
      WEBUI_LISTEN_ADDRESS: 0.0.0.0:8081
    ports:
      - "${OPENCODE2API_PORT:-8180}:8180"
      - "${OPENCODE2API_WEBUI_PORT:-8081}:8081"
    volumes:
      - .:/app
      - opencode2api-state:/var/lib/opencode2api
      - go-build-cache:/root/.cache/go-build
      - go-module-cache:/go/pkg/mod
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8180/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  opencode2api-state:
  go-build-cache:
  go-module-cache:
